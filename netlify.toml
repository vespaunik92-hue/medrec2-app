[build]
  # Perintah baru: Cari kata [netlify], jika KETEMU maka berikan sinyal JALAN (exit 1)
  # Jika TIDAK KETEMU, berikan sinyal BATAL (exit 0)
  ignore = "git log -1 --pretty=%B | grep -Fq '[netlify]'; if [ $? -eq 0 ]; then exit 1; else exit 0; fi"